<?xml version="1.0" encoding="UTF-8"?>

<project>
    <artifactId>${application.id}</artifactId>
    <groupId>${application.id}</groupId>
    <name>The ${application.name}</name>
    <currentVersion>${application.version}</currentVersion>
    <shortDescription>The Controle de Acesso</shortDescription>
    <description>
        Contains the Controle de Acesso Project.
    </description>
    <package>${application.package}</package>
    <developers>
        <developer>
              <name>MDArte</name>
              <id></id>
              <email></email>
        </developer>
    </developers>
    <dependencies>
        <dependency>
            <groupId>commons-jelly</groupId>
            <artifactId>commons-jelly-tags-interaction</artifactId>
            <version>${commons.jelly.version}</version>
        </dependency>
        <dependency>
            <groupId>jboss</groupId>
            <artifactId>jbosssx</artifactId>
            <version>${jboss.version}</version>
            <type>jar</type>
        </dependency>
        <dependency>
            <groupId>common-classes</groupId>
            <artifactId>CommonClasses</artifactId>
            <version>${common.classes.version}</version>
            <type>jar</type>
            <properties>
                <application.dependency>true</application.dependency>
                <jar.manifest.classpath>true</jar.manifest.classpath>
            </properties>
        </dependency>
    	    </dependencies>
</project>
